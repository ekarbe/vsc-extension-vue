# VSCode + VueJS Extension Template

This is a template for a Visual Studio Code extension with VueJS integration.

## Usage

Clone the repository

`git clone git@github.com:ekarbe/vsc-extension-vue.git`

Install packages

`npm install`

Build Vue App

`npm run build`

Start extension debugger

`F5`

Or build the extension and install it

`vsce package`